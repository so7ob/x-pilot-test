{
  "name": "x-pilot-extension",
  "private": true,
  "version": "1.5.6",
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build && vite build --config vite.content.config.ts",
    "test": "node --experimental-strip-types --test tests/*.test.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/chrome": "latest",
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest"
  }
}
